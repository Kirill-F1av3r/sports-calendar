server:
  port: ${SERVER_PORT:8080}

services:
  auth:
    url: ${AUTH_SERVICE_URL:http://localhost:8081}
  calendar:
    url: ${CALENDAR_SERVICE_URL:http://localhost:8082}

security:
  jwt:
    secret: ${GATEWAY_JWT_SECRET:dev-secret-change-me-32-bytes-minimum}