spring:
  datasource:
    url: ${AUTH_DB_URL}
    username: ${AUTH_DB_USERNAME}
    password: ${AUTH_DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  security:
    jwt:
      secret: ${AUTH_JWT_SECRET}
      accessTokenSeconds: ${AUTH_JWT_ACCESS_TOKEN_SECONDS:900}
